<UserControl x:Class="FolderWatcher.Views.MainView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:FolderWatcher"
             xmlns:tb="http://www.hardcodet.net/taskbar"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <tb:TaskbarIcon ToolTipText="hello world"
                        IconSource="../Icons/FolderWatcher.ico">
            <tb:TaskbarIcon.TrayToolTip>
                <Grid Width="100" Height="100" Background="White">
                    <TextBlock Text="Super Tool Tip" />
                </Grid>
            </tb:TaskbarIcon.TrayToolTip>
            <tb:TaskbarIcon.TrayPopup>
                <Grid Width="200" Height="200" Background="Red">
                    <TextBlock Text="Super Tool Tip" />
                </Grid>
            </tb:TaskbarIcon.TrayPopup>
        </tb:TaskbarIcon>
    </Grid>
</UserControl>
